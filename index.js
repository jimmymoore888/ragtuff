/**
 * RagTuff
 * Early infrastructure for safe, non-custodial wallet connectivity
 */

export function createRagTuff() {
  return {
    name: "RagTuff",
    version: "0.0.1",
    status: "initialized"
  };
}
